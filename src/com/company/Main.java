package com.company;

public class Main {

    public static String hyphenMe(String str) {
        if (str.length()%2 == 0)
        {
            String evenH = str.substring(0, str.length()/2);
            String evenH2 = str.substring(str.length()/2, str.length());
            String palinH = "";
            for (int h = evenH.length(); h >= 0; h--) {
                
            }
        }
        else
        {}
    }
}
